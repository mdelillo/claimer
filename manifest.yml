applications:
- name: claimer
  buildpack: go_buildpack
  health-check-type: process
  no-route: true
  env:
    GOPACKAGENAME: github.com/mdelillo/claimer
    GOVERSION: go1.10
    SSH_KNOWN_HOSTS: ./known_hosts
    LOG_LEVEL:
    API_TOKEN:
    CHANNEL_ID:
    REPO_URL:
    DEPLOY_KEY:
    TRANSLATION_FILE:
